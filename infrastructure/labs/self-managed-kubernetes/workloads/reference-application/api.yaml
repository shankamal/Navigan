apiVersion: apps/v1
kind: Deployment
metadata:
  name: reference-api
spec:
  replicas: 2
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate: {maxUnavailable: 0, maxSurge: 1}
  selector:
    matchLabels: {app: reference-api}
  template:
    metadata:
      labels:
        app: reference-api
        tier: application
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
    spec:
      serviceAccountName: reference-api
      automountServiceAccountToken: false
      securityContext:
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      terminationGracePeriodSeconds: 20
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels: {app: reference-api}
      initContainers:
        - name: wait-for-data-services
          image: busybox:1.37
          command:
            - sh
            - -c
            - until nc -z postgres 5432 && nc -z redis 6379; do sleep 2; done
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: {drop: ["ALL"]}
      containers:
        - name: api
          image: python:3.12-alpine
          command: ["python", "/app/server.py"]
          envFrom:
            - configMapRef:
                name: reference-settings
          env:
            - name: PYTHONDONTWRITEBYTECODE
              value: "1"
          ports:
            - name: http
              containerPort: 8080
          startupProbe:
            httpGet: {path: /healthz, port: http}
            failureThreshold: 30
            periodSeconds: 2
          readinessProbe:
            httpGet: {path: /readyz, port: http}
            periodSeconds: 5
          livenessProbe:
            httpGet: {path: /healthz, port: http}
            periodSeconds: 10
          resources:
            requests: {cpu: 100m, memory: 64Mi}
            limits: {cpu: 500m, memory: 256Mi}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 10001
            capabilities: {drop: ["ALL"]}
          volumeMounts:
            - {name: source, mountPath: /app, readOnly: true}
            - {name: secret, mountPath: /var/run/secrets/reference, readOnly: true}
            - {name: tmp, mountPath: /tmp}
        - name: heartbeat-sidecar
          image: busybox:1.37
          command: ["sh", "-c", "while true; do date -u; sleep 30; done"]
          resources:
            requests: {cpu: 5m, memory: 8Mi}
            limits: {cpu: 25m, memory: 32Mi}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 10002
            capabilities: {drop: ["ALL"]}
      volumes:
        - name: source
          configMap:
            name: reference-api-source
            defaultMode: 0444
        - name: secret
          secret:
            secretName: reference-secrets
            defaultMode: 0440
        - name: tmp
          emptyDir:
            sizeLimit: 64Mi
---
apiVersion: v1
kind: Service
metadata:
  name: reference-api
spec:
  selector: {app: reference-api}
  ports:
    - name: http
      port: 8080
      targetPort: http
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: reference-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: reference-api
  minReplicas: 2
  maxReplicas: 6
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 120
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
